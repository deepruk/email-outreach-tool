# Mailflow Scheduler MVP

Build a single-workspace dashboard for Gmail inbox connections, recipients, templates, campaigns, randomized 10–20 minute send gaps, and send history. Real Google OAuth/Gmail API support is wired; seeded preview data and the seeded campaign send are **MOCKED** until a user completes Gmail OAuth.