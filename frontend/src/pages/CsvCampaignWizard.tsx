import { useMemo, useState } from "react";
import { useMutation, useQuery } from "@tanstack/react-query";
import { Link, useNavigate } from "react-router-dom";
import { toast } from "sonner";
import { ArrowLeft, ArrowRight, Check, Clock3, FileSpreadsheet, FlaskConical, Inbox as InboxIcon, Mail, ShieldCheck, Upload } from "lucide-react";
import { ApiError, apiGet, apiPost, apiUpload } from "@/lib/api";
import type { CsvCampaign, CsvCampaignCreate, CsvCampaignLaunchResponse, CsvCampaignPreview, CsvSource, Inbox, SequenceStepInput } from "@/lib/types";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Toaster } from "@/components/ui/sonner";
import { TestEmailDialog } from "@/components/rohly/TestEmailDialog";
const wizardSteps=["Upload CSV","Map columns","Select inboxes","Configure sequence","Preview","Launch"];
const errorMessage=(error:unknown)=>error instanceof ApiError?(typeof(error.body as {detail?:unknown}|null)?.detail==="string"?(error.body as {detail:string}).detail:`Request failed (${error.status})`):"Request failed. Please try again.";
const guess=(columns:string[],terms:string[])=>columns.find(column=>{const normalized=column.toLowerCase().replace(/[^a-z0-9]/g,"");return terms.some(term=>normalized.includes(term));})??"";
const defaultSteps=(columns:string[]):SequenceStepInput[]=>[
{key:"initial",label:"Initial email",subject_column:guess(columns,["firstsubject","initialsubject","subject1"]),body_column:guess(columns,["firstbody","initialbody","body1"]),day_offset:0,send_time:"10:30"},
{key:"follow_up_1",label:"Follow-up 1",subject_column:guess(columns,["followup1subject","followupsubject1"]),body_column:guess(columns,["followup1body","followupbody1"]),day_offset:2,send_time:"11:00"},
{key:"follow_up_2",label:"Follow-up 2",subject_column:guess(columns,["followup2subject","followupsubject2"]),body_column:guess(columns,["followup2body","followupbody2"]),day_offset:3,send_time:"14:00"}];
function ColumnSelect({label,value,columns,onChange,optional=false,testId}:{label:string;value:string;columns:string[];onChange:(value:string)=>void;optional?:boolean;testId:string}){return <label className="block"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="h-10 w-full rounded-md border border-zinc-200 bg-white px-3 text-sm" data-testid={testId}><option value="">{optional?"Not mapped":"Select a column"}</option>{columns.map(column=><option key={column} value={column}>{column}</option>)}</select></label>}
export default function CsvCampaignWizard(){
const navigate=useNavigate();const[step,setStep]=useState(0);const[source,setSource]=useState<CsvSource|null>(null);const[name,setName]=useState("Personalized outreach");const[emailColumn,setEmailColumn]=useState("");const[firstNameColumn,setFirstNameColumn]=useState("");const[companyColumn,setCompanyColumn]=useState("");const[statusColumn,setStatusColumn]=useState("");const[selectedInboxes,setSelectedInboxes]=useState<string[]>([]);const[sequence,setSequence]=useState<SequenceStepInput[]>([]);const[timezone,setTimezone]=useState("Asia/Kolkata");const[minGapMinutes,setMinGapMinutes]=useState(10);const[maxGapMinutes,setMaxGapMinutes]=useState(20);const[sendingWindowStart,setSendingWindowStart]=useState("09:00");const[sendingWindowEnd,setSendingWindowEnd]=useState("18:00");const[sendingDays,setSendingDays]=useState<number[]>([0,1,2,3,4]);const[preview,setPreview]=useState<CsvCampaignPreview|null>(null);const[createdCampaign,setCreatedCampaign]=useState<CsvCampaign|null>(null);const[testOpen,setTestOpen]=useState(false);
const inboxesQuery=useQuery({queryKey:["inboxes"],queryFn:()=>apiGet<Inbox[]>("/workspace/inboxes")});const connectedInboxes=(inboxesQuery.data??[]).filter(inbox=>inbox.status==="connected");
const payload=useMemo<CsvCampaignCreate|null>(()=>source?{name,source_id:source.id,email_column:emailColumn,first_name_column:firstNameColumn||null,company_column:companyColumn||null,status_column:statusColumn||null,inbox_ids:selectedInboxes,steps:sequence,timezone,min_gap_minutes:minGapMinutes,max_gap_minutes:maxGapMinutes,sending_window_start:sendingWindowStart,sending_window_end:sendingWindowEnd,sending_days:sendingDays}:null,[source,name,emailColumn,firstNameColumn,companyColumn,statusColumn,selectedInboxes,sequence,timezone,minGapMinutes,maxGapMinutes,sendingWindowStart,sendingWindowEnd,sendingDays]);
const uploadMutation=useMutation({mutationFn:(file:File)=>{const form=new FormData();form.append("file",file);return apiUpload<CsvSource>("/csv/sources",form)},onSuccess:r=>{setSource(r);setEmailColumn(guess(r.columns,["email","recipientemail"]));setFirstNameColumn(guess(r.columns,["firstname","first"]));setCompanyColumn(guess(r.columns,["company","organization"]));setStatusColumn(guess(r.columns,["status"]));setSequence(defaultSteps(r.columns));setStep(1);toast.success(`${r.row_count} leads loaded`)},onError:e=>toast.error(errorMessage(e))});
const previewMutation=useMutation({mutationFn:(data:CsvCampaignCreate)=>apiPost<CsvCampaignPreview>("/csv/campaigns/preview",data),onSuccess:r=>{setPreview(r);setStep(4)},onError:e=>toast.error(errorMessage(e))});
const createMutation=useMutation({mutationFn:(data:CsvCampaignCreate)=>apiPost<CsvCampaign>("/csv/campaigns",data),onSuccess:r=>{setCreatedCampaign(r);setStep(5)},onError:e=>toast.error(errorMessage(e))});
const launchMutation=useMutation({mutationFn:(id:string)=>apiPost<CsvCampaignLaunchResponse>(`/csv/campaigns/${id}/launch`),onSuccess:r=>{toast.success(`${r.scheduled_count} exact emails scheduled`);setTimeout(()=>navigate("/"),800)},onError:e=>toast.error(errorMessage(e))});
const updateSequence=(i:number,p:Partial<SequenceStepInput>)=>setSequence(c=>c.map((x,j)=>j===i?{...x,...p}:x));
const goNext=()=>{if(!source||!payload)return;if(step===1&&(!emailColumn||sequence.some(x=>!x.subject_column||!x.body_column)))return toast.error("Map email, subject, and body columns for every step");if(step===2&&!selectedInboxes.length)return toast.error("Select at least one inbox");if(step===3){if(!sequence.length||sequence[0].day_offset!==0)return toast.error("Initial email must use 0 days");if(sequence.slice(1).some(x=>!Number.isFinite(x.day_offset)||x.day_offset<1))return toast.error("Each follow-up must be at least 1 day after the previous email");if(!Number.isFinite(minGapMinutes)||!Number.isFinite(maxGapMinutes)||minGapMinutes<1||maxGapMinutes<1||minGapMinutes>maxGapMinutes)return toast.error("Enter a valid minimum/maximum email gap");if(sendingWindowStart>=sendingWindowEnd)return toast.error("Working-hours start must be before the end time");if(!sendingDays.length)return toast.error("Select at least one working day");return previewMutation.mutate(payload)}if(step===4)return createMutation.mutate(payload);setStep(s=>Math.min(5,s+1))};
return <div className="min-h-svh bg-[#f4f4f5] text-zinc-950"><header className="border-b border-zinc-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6"><Link to="/" className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-md bg-zinc-950 text-white"><Mail size={16}/></div><div><p className="font-heading text-base font-semibold">Mailflow</p><p className="text-[10px] uppercase tracking-[0.12em] text-zinc-400">Deepanshu's workspace</p></div></Link><Link to="/" className="inline-flex h-8 items-center gap-2 rounded-md px-3 text-sm"><ArrowLeft size={14}/> Exit setup</Link></div></header><main className="mx-auto max-w-7xl px-4 py-7 sm:px-6"><div className="grid gap-7 lg:grid-cols-[250px_1fr]"><aside><p className="mb-4 text-[10px] font-semibold uppercase tracking-[0.14em] text-zinc-400">Campaign setup</p><div className="space-y-1">{wizardSteps.map((label,i)=><div key={label} className={`flex items-center gap-3 rounded-md px-3 py-2.5 ${i===step?"bg-zinc-950 text-white":i<step?"text-emerald-700":"text-zinc-400"}`}><span className="flex size-6 items-center justify-center rounded-full bg-zinc-200 text-[10px] font-semibold">{i<step?<Check size={12}/>:i+1}</span><span className="text-sm font-medium">{label}</span></div>)}</div><div className="mt-6 border border-emerald-200 bg-emerald-50 p-3"><div className="flex gap-2"><ShieldCheck size={15}/><div><p className="text-xs font-semibold">Exact-copy guarantee</p><p className="mt-1 text-[11px] leading-relaxed">Mailflow reads, schedules, and sends your CSV text without generating or rewriting it.</p></div></div></div></aside><section className="min-w-0"><div className="mb-6"><p className="text-[11px] font-semibold uppercase tracking-[0.12em] text-zinc-500">Step {step+1} of 6</p><h1 className="mt-2 font-heading text-3xl font-semibold tracking-tight">{wizardSteps[step]}</h1></div>
{step===0&&<div className="border border-zinc-200 bg-white p-5"><label className="flex min-h-80 cursor-pointer flex-col items-center justify-center border border-dashed border-zinc-300 bg-zinc-50 px-6 text-center"><Upload size={19}/><h2 className="mt-5 font-heading text-xl font-medium">Upload your lead CSV</h2><p className="mt-2 max-w-md text-sm text-zinc-500">The first row must contain column names. Every lead keeps its own subject and body exactly as uploaded.</p><span className="mt-5 rounded-md bg-zinc-950 px-4 py-2 text-sm font-medium text-white">{uploadMutation.isPending?"Reading CSV…":"Choose CSV file"}</span><input type="file" accept=".csv,text/csv" className="sr-only" onChange={e=>{const f=e.target.files?.[0];if(f)uploadMutation.mutate(f)}}/></label></div>}
{step===1&&source&&<div className="space-y-4"><div className="border border-zinc-200 bg-white p-4"><p className="text-sm font-medium">{source.filename}</p><p className="text-xs text-zinc-500">{source.row_count} leads · {source.columns.length} columns</p></div><div className="border border-zinc-200 bg-white p-5"><h2 className="font-heading text-lg font-medium">Lead identity</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><ColumnSelect label="Recipient email" value={emailColumn} columns={source.columns} onChange={setEmailColumn} testId="map-email-column"/><ColumnSelect label="First name" value={firstNameColumn} columns={source.columns} onChange={setFirstNameColumn} optional testId="map-first-name-column"/><ColumnSelect label="Company" value={companyColumn} columns={source.columns} onChange={setCompanyColumn} optional testId="map-company-column"/><ColumnSelect label="Existing status" value={statusColumn} columns={source.columns} onChange={setStatusColumn} optional testId="map-status-column"/></div></div><div className="border border-zinc-200 bg-white p-5"><h2 className="font-heading text-lg font-medium">Personalized message columns</h2><div className="mt-5 space-y-5">{sequence.map((item,i)=><div key={item.key}><p className="mb-3 text-sm font-medium">{item.label}</p><div className="grid gap-4 sm:grid-cols-2"><ColumnSelect label="Subject column" value={item.subject_column} columns={source.columns} onChange={v=>updateSequence(i,{subject_column:v})} testId={`map-${item.key}-subject`}/><ColumnSelect label="Body column" value={item.body_column} columns={source.columns} onChange={v=>updateSequence(i,{body_column:v})} testId={`map-${item.key}-body`}/></div></div>)}</div></div></div>}
{step===2&&<div className="border border-zinc-200 bg-white"><div className="border-b border-zinc-200 p-5"><h2 className="font-heading text-lg font-medium">Choose sending inboxes</h2></div><div className="max-h-[430px] divide-y divide-zinc-100 overflow-y-auto">{connectedInboxes.length?connectedInboxes.map(inbox=><label key={inbox.id} className="flex cursor-pointer items-center gap-4 p-4"><input type="checkbox" checked={selectedInboxes.includes(inbox.id)} onChange={()=>setSelectedInboxes(c=>c.includes(inbox.id)?c.filter(x=>x!==inbox.id):[...c,inbox.id])}/><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{inbox.display_name}</p><p className="truncate text-xs text-zinc-500">{inbox.email}</p></div><p className="text-xs">{inbox.daily_sending_limit} / day</p></label>):<div className="p-8 text-center text-sm">No inboxes available</div>}</div></div>}
{step===3&&<div className="space-y-4"><div className="border border-zinc-200 bg-white p-5"><label className="block"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">Campaign name</span><Input value={name} onChange={e=>setName(e.target.value)}/></label><label className="mt-4 block"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">Timezone</span><select value={timezone} onChange={e=>setTimezone(e.target.value)} className="h-10 w-full rounded-md border border-zinc-200 bg-white px-3 text-sm"><option value="Asia/Kolkata">India Standard Time</option><option value="America/New_York">Eastern Time</option><option value="America/Los_Angeles">Pacific Time</option><option value="Europe/London">London</option><option value="UTC">UTC</option></select></label><div className="mt-4 rounded-md border border-zinc-200 bg-zinc-50 p-4"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">Working hours</span><p className="mb-4 text-xs text-zinc-500">Emails will only be sent inside this daily time window.</p><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-xs font-medium">Start time</span><Input type="time" value={sendingWindowStart} onChange={e=>setSendingWindowStart(e.target.value)}/></label><label><span className="mb-2 block text-xs font-medium">End time</span><Input type="time" value={sendingWindowEnd} onChange={e=>setSendingWindowEnd(e.target.value)}/></label></div><div className="mt-4"><span className="mb-2 block text-xs font-medium">Working days</span><div className="flex flex-wrap gap-2">{[[0,"Mon"],[1,"Tue"],[2,"Wed"],[3,"Thu"],[4,"Fri"],[5,"Sat"],[6,"Sun"]].map(([day,label])=>{const d=Number(day),enabled=sendingDays.includes(d);return <label key={d} className="flex cursor-pointer items-center gap-2 rounded-md border px-3 py-2 text-xs"><input type="checkbox" checked={enabled} onChange={()=>setSendingDays(c=>enabled?c.filter(x=>x!==d):[...c,d].sort((a,b)=>a-b)}/><span>{label}</span></label>})}</div></div><p className="mt-3 text-[11px] text-zinc-500">Default: Monday–Friday, 9:00 AM–6:00 PM.</p></div><div className="mt-4 rounded-md border border-zinc-200 bg-zinc-50 p-4"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">Random email gap</span><p className="mb-4 text-xs text-zinc-500">Each email is randomly scheduled between the minimum and maximum gap.</p><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-xs font-medium">Minimum gap (minutes)</span><Input type="number" min="1" max="1440" value={minGapMinutes} onChange={e=>setMinGapMinutes(Number(e.target.value))}/></label><label><span className="mb-2 block text-xs font-medium">Maximum gap (minutes)</span><Input type="number" min="1" max="1440" value={maxGapMinutes} onChange={e=>setMaxGapMinutes(Number(e.target.value))}/></label></div><p className="mt-3 text-[11px] text-zinc-500">Default: 10–20 minutes.</p></div></div>{sequence.map((item,i)=><div key={item.key} className="border border-zinc-200 bg-white p-5"><div className="flex items-center gap-3"><Clock3 size={16}/><h2 className="text-sm font-medium">{item.label}</h2></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">{i===0?"Days after campaign start":"Days after previous email"}</span><Input type="number" min={i===0?"0":"1"} value={item.day_offset} onChange={e=>updateSequence(i,{day_offset:Number(e.target.value)})}/></label><label><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[0.09em] text-zinc-500">Send time</span><Input type="time" value={item.send_time} onChange={e=>updateSequence(i,{send_time:e.target.value})}/></label></div></div>)}</div>}
{step===4&&preview&&<div className="space-y-4">{preview.leads.map(lead=><article key={lead.row_index} className="border border-zinc-200 bg-white p-5"><h2 className="font-heading text-lg font-medium">{lead.first_name||lead.email}{lead.company?` — ${lead.company}`:""}</h2><p className="mt-1 text-xs text-zinc-500">{lead.email} · CSV row {lead.row_index}</p>{lead.steps.map(message=><div key={message.key} className="mt-4 border-t border-zinc-100 pt-4"><p className="text-xs font-medium">{message.label}</p><p className="mt-2 text-sm font-medium">{message.subject}</p><p className="mt-2 whitespace-pre-wrap text-sm text-zinc-600">{message.body}</p></div>)}</article>)}</div>}
{step===5&&<div className="border border-zinc-200 bg-white p-6"><Check size={20}/><h2 className="mt-5 font-heading text-2xl font-semibold">Ready to schedule</h2><p className="mt-2 text-sm text-zinc-500">{preview?.valid_count??0} leads will be scheduled across {selectedInboxes.length} inbox{selectedInboxes.length===1?"":"es"}.</p><Button onClick={()=>createdCampaign&&launchMutation.mutate(createdCampaign.id)} disabled={!createdCampaign||launchMutation.isPending} className="mt-7 gap-2"><Mail size={15}/>{launchMutation.isPending?"Scheduling…":"Launch campaign"}</Button></div>}
{step>0&&step<5&&<div className="mt-6 flex items-center justify-between border-t border-zinc-200 pt-5"><Button type="button" variant="ghost" onClick={()=>setStep(s=>Math.max(0,s-1))}><ArrowLeft size={14}/> Back</Button><Button type="button" onClick={goNext} disabled={previewMutation.isPending||createMutation.isPending}>{step===4?"Create campaign":step===3?"Build preview":"Continue"}<ArrowRight size={14}/></Button></div>}</section></div></main>{step===5&&createdCampaign&&<div className="fixed bottom-5 right-5 z-30"><Button onClick={()=>setTestOpen(true)} variant="outline"><FlaskConical size={14}/> Send test before launch</Button></div>}{testOpen&&createdCampaign&&<TestEmailDialog campaignId={createdCampaign.id} inboxCount={createdCampaign.inbox_ids.length} onClose={()=>setTestOpen(false)}/>}<Toaster position="bottom-right" richColors/></div>}
